#!/bin/bash

# $1 is the location (ends with /)
# $2 is the name to use
# $3 is the domain name or server
# $4 is the title

dir2html "$@"

# Recursive
for d in $1*/ ; do
	# dname is only the last directory name
	dname=$(basename -- "$d")
	echo "$dname"
	dir2html "$d" "$2/$dname" "$3" "$4"
done
