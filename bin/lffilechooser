#!/bin/bash
if [ $# -eq 0 ]; then
  lf -last-dir-path ~/.cache/lf/last-location.txt -print-selection $(cat ~/.cache/lf/last-location.txt)
else
  lf -last-dir-path ~/.cache/lf/last-location.txt -print-selection -selection-path "$1" $(cat ~/.cache/lf/last-location.txt)
fi
